<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <LangVersion>preview</LangVersion>
    <Description>Generates builder classes for testing and/or seed data.</Description>

    <PackageReleaseNotes>This version generates a usable Builder, but there is work left to do. It's a coding pattern I've used for years, but its handling of collections is still naive, and assumes they can all be completely overwritten. A future release will clear and repopulate collections where possible.

v0.1.1
- Preview/Alpha release

v0.1.0
- Initial Release
    </PackageReleaseNotes>
    <RepositoryUrl>https://github.com/MelGrubb/BuilderGenerator</RepositoryUrl>
    <NeutralLanguage />
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="3.8.0-3.final" PrivateAssets="all" />
    <PackageReference Include="Microsoft.CodeAnalysis.Analyzers" Version="3.0.0" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\BuilderGenerator.Common\BuilderGenerator.Common.csproj" />
  </ItemGroup>

</Project>